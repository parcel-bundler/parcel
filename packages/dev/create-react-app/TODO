* Don't just simply copy package.json, insert the name of the package (probably path.basename(packagePath))
* Make initial commit
* Make log messages nice (e.g. colors)
* Write more thorough instructions
* Test with npm
* Add flowtypes
* Contribute change to commander to flow-typed
