// @flow strict-local;
import assert from 'assert';

describe('incremental bundling for dependency changes', function() {
  it('should produce a bundlegraph equivalent to traditional bundling result', async function() {
    //bundle something or call parcel, force a change on a file whiel running, capture result
    //do same with incremental flag on
    //compare the bundles from both runs!
  });
});
