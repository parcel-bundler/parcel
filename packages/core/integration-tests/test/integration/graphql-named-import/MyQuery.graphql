# import UserFragment, Address from './fragments.graphql'
# import * from "./otherFragments.graphql"

query MyQuery {
    user(id: 6) {
        ...UserFragment
        address {
            ...Address
        }
    }
}
