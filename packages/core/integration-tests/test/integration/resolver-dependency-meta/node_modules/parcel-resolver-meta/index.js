const {Resolver} = require('@parcel/plugin');

module.exports = new Resolver({
  resolve({dependency, options, filePath}) {
    if(dependency.moduleSpecifier === "foo"){
      dependency.meta.customValue = 1234;

      return {isExcluded: true};
    }
  }
});
