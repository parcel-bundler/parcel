const {Runtime} = require('@atlaspack/plugin');

exports.default = new Runtime({
  async apply({options}) {
    return {
      filePath: __filename,
      code: 'console.log("runtime injected");',
      isEntry: true
    }
  }
})
