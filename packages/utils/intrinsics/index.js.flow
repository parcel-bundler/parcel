// @flow
/** Requires an already loaded module by public id. */
declare export function requireModuleById(id: string): any;
/** Loads an ESM bundle from a URL relative to the target dist dir. */
declare export function loadESMBundle(url: string): Promise<any>;
