{
  "name": "@parcel/scss-example",
  "version": "2.0.0-beta.1",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "PARCEL_WORKERS=0 parcel build src/style.scss --no-minify --no-scope-hoist --no-cache",
    "clean-demo": "rm -rf .parcel-cache dist && yarn demo"
  },
  "devDependencies": {
    "@parcel/babel-register": "2.0.0-beta.1",
    "@parcel/core": "2.0.0-beta.1"
  }
}
