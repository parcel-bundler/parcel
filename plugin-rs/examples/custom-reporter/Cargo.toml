[workspace]

[package]
name = "custom-reporter-rs"
version = "0.1.0"
edition = "2024"

[lib]
name = "custom_reporter_rs"
crate-type = ["cdylib"]

[dependencies]
parcel-plugin = { path = "../../../crates/parcel-plugin" }
