{
  "version": "my-version",
  "scripts": {
    "start": "rm -rf dist && parcel build index.html && http-server dist"
  },
  "dependencies": {
    "http-server": "*",
    "parcel-bundler": "^1.0.0"
  },
  "engines": {
    "node": "9.x"
  }
}
