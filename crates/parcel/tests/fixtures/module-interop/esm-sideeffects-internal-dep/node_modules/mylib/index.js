import { create } from './lib/create.js';

// normalize is re-exported but not used by the consumer
export { normalize } from './lib/normalize.js';

// result uses create, which internally uses normalize
export const result = create('HELLO');
